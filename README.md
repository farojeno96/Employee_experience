# Employee_experience
#     This repository contains the data and codes used to generate our results
#     SPSS_1 contains the raw data which are filter and processed using SPSS (SPSS_code_EX) to create principal components regressions based on the principal component scores
#     From SPSS we export PCA_and_interactions_FINAL.sav containing the principal component scores. 
#     This is analyzed in R (Modeling.R) to crosscheck the results of SPSS and to estimate variance-covariance matrices
